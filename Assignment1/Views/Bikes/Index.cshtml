@model IEnumerable<Assignment1.Models.Bike>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Bike1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Bike2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Bike3)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Bike1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Bike2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Bike3)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Bikes }) |
            @Html.ActionLink("Details", "Details", new { id=item.Bikes }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Bikes })
        </td>
    </tr>
}

</table>
