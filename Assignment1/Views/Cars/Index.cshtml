@model IEnumerable<Assignment1.Models.Car>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Car1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Car2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Car3)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Car1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Car2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Car3)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Cars }) |
            @Html.ActionLink("Details", "Details", new { id=item.Cars }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Cars })
        </td>
    </tr>
}

</table>
